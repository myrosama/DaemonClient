{
  "name": "daemonclient-processor",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless HEIC to JPEG converter — one free function per user, no server to run",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4",
    "libheif-js": "^1.19.8",
    "pngjs": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
